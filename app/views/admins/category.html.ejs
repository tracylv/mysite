<div class="category">
    <div class="ro whitebg w12">
     <% if(session_obj.userrole == "super")
        { %>
        <div class="ce bluebg">
           <a href="/admins/list?issuper=true">高级管理员管理</a>
        </div>
     <% }
        if(session_obj.userrole == "super" || session_obj.userrole == "senior")
        { %>
        <div class="ce greenbg">
           <a href="/admins/list">管理员管理</a>
        </div>
     <% }
        if(session_obj.userrole == "super" || session_obj.userrole == "senior" || session_obj.userrole == "junior")
        { %>
        <div class="ce blackbg">
           <a href="/users/list">用户管理</a>
        </div>
     <% } %>
    </div>
</div>
